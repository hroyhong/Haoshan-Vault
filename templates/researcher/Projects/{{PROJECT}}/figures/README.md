# figures

Output plots, schematics, illustrations for {{PROJECT}}.

Naming: `fig-<number>-<short-name>.<ext>`. Source files (Inkscape, Affinity, scripts) go next to their output with same base name.

Reference figures by filename in drafts so they don't drift.
