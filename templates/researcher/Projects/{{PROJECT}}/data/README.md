# data

Datasets used in {{PROJECT}}.

Sub-structure:
- `raw/` — original sources
- `processed/` — derived datasets you actually analyze

Anything too large for git goes outside the vault; leave a pointer (path or URL) in this README or a `data-locations.md`.
