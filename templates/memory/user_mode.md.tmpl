---
name: user-mode
description: {{MODE}}-mode defaults and red flags for {{NAME}}'s vault.
metadata:
  type: user
---

Mode: **{{MODE}}**

See `{{VAULT_PATH}}/CLAUDE.md` for the full mode-specific CoS persona block. This memory entry is just a fast pointer so first-session context picks up the right framing.

For founder mode, default bias: ship beats discuss, sprint-driven, runway-aware.
For researcher mode, default bias: deadline-driven, citation-aware, defense protects timeline.

If {{NAME}} later asks to switch modes, do not edit this file. Update root `CLAUDE.md` mode frontmatter and re-cascade. Then update this memory.
