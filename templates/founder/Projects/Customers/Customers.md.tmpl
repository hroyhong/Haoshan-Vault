# Customers

Customer accounts, calls, retention notes for {{STARTUP}}.

## Active accounts

(One sub-file per major account once relationships exist. Currently empty.)

## Call log

(Date + customer + outcome + next step.)
