# {{STARTUP}}

(Project description. What is {{STARTUP}}? Who is it for? What's the one sentence that explains why it exists?)

## Current focus

See `[[plan]]` for strategy, `[[status]]` for sprint state, `[[log]]` for handoff.

## Characters

- `[[CTO]]` — builds, codes, deploys
- `CPO/` — product workspace ({{NAME}}'s, not AI-orchestrated)
- `CMO/` — marketing workspace ({{NAME}}'s, not AI-orchestrated)
- `Outreach/` — investor + partner + hire outreach
